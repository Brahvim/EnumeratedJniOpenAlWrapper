package com.brahvim.openal;

/* `package` */ class AlJniContact {

    private AlJniContact() {
        throw new UnsupportedOperationException("Sorry, but `"
                + this.getClass().getSimpleName()
                + "` is a helper class and should not be instantiated.");
    }

}
